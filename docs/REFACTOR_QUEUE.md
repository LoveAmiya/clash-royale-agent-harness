# Remaining Refactor Queue



This queue is the continuation ledger for the repository cleanup. It covers

unfinished Phase 2, Phase 4, Phase 5, Phase 6, plus earlier gaps that should be

closed before the project is considered healthy.



## Operating Rules



- Work one small behavior-preserving slice at a time.

- If a row is marked `doing`, continue that row before starting a new one.

- If no row is `doing`, pick the first `todo` row.

- When starting a row, change its status to `doing` in this file.

- When the row is complete and verified, change its status to `done` and add a

  short evidence note.

- If the slice is blocked by credentials, live Supercell access, a running

  collector, or missing user authority, mark `blocked` with the exact reason.

- If a slice finishes early, continue with the next row in the same run.

- Do not commit, push, bulk-stage, or touch private runtime data.

- Do not change collection strategy, RAG behavior, public API contracts, or

  scheduled-task behavior unless that row explicitly says so.



## Standard Verification



- Docs-only slice: `powershell -ExecutionPolicy Bypass -File .\scripts\check_repo.ps1 -SkipTests`

- Import/package slice: `.\.venv\Scripts\python.exe -m unittest discover -s tests`

- Runtime, QA/RAG, collector, snapshot, or publication slice: `powershell -ExecutionPolicy Bypass -File .\run_tests.ps1`

- Always inspect `git status --short` before finishing. Data, logs, tokens,

  SQLite, JSONL traces, generated reports, player tags, and raw battle artifacts

  must not be staged or newly tracked.



## Approximate Count



Current plan: about 39 small slices. The count may shrink if adjacent docs-only

items are safely finished together, or grow if a monolith split exposes hidden

contracts that need a separate regression test.



## Queue



| # | Phase | Status | Slice | Evidence / notes |

|---:|---|---|---|---|

| 1 | Earlier gap | done | Run a fresh repo health audit: current `git status`, ignored private outputs, oversized modules, entry commands, and current test baseline; update this queue if the facts changed. | 2026-08-17 heartbeat audit: private outputs remained ignored; six root monoliths still 1280-2043 lines; entry scripts present; `scripts/check_repo.ps1 -SkipRuff` passed with 1011 tests, 1 skipped. |

| 2 | Earlier gap | done | Audit all root entry points that import packaged modules without first loading `app_config`; add focused tests for any remaining direct-entry import failures. | Added `tests/test_entrypoint_package_bootstrap.py`; targeted `test_entrypoint_package_bootstrap` and `test_evaluation_entrypoints` passed. |

| 3 | Earlier gap | done | Add a dev-tooling dependency path, such as `requirements-dev.txt`, for Ruff/pytest/mypy without making CI depend on live providers. | Added `requirements-dev.txt`; `test_repo_tooling_config`, `test_entrypoint_package_bootstrap`, and `test_evaluation_entrypoints` passed. |

| 4 | Earlier gap | done | Make `scripts/check_repo.ps1` report a concise summary of checks run, skipped advisory tools, and private-path scan results. | Added check-run, skipped-tool, and private-path summaries; `test_check_repo_script`, tooling/bootstrap tests, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |

| 5 | Phase 2 | done | Extract the remaining runtime startup/lifespan orchestration from `runtime_multi.py` into `clashroyale_agent.api.startup` or lifecycle helpers, preserving startup behavior. | Added `initialize_runtime_services` for metrics, feedback, quota creation/probe; lifecycle/package tests and `check_repo.ps1 -SkipRuff` passed with 1014 tests, 1 skipped. |

| 6 | Phase 2 | done | Extract RAG preheat orchestration from `runtime_multi.py` into an API preheat/lifecycle module with targeted regression tests. | Added `clashroyale_agent.api.rag_preheat` with explicit dependency bundle; RAG preheat/API package/production tests and `check_repo.ps1 -SkipRuff` passed with 1014 tests, 1 skipped. |

| 7 | Phase 2 | done | Extract live snapshot refresh orchestration from `runtime_multi.py` into a snapshot lifecycle module without changing cooldown or publication behavior. | Added `api.snapshot_lifecycle` with explicit runtime dependencies; cache, incomplete/source-exhausted cooldown, publication, RAG activation, follower, and one-shot refresh remain behind compatibility facades. Package/snapshot/deployment tests and `run_tests.ps1` passed: 1015 tests, 1 skipped. |

| 8 | Phase 2 | done | Extract dataset dependency resolution and catalog route support from `runtime_multi.py`, keeping dataset-scope responses identical. | Added `api.dataset_runtime` with explicit dataset/RAG/repository dependencies; catalog, structured repository, and rolling retriever keep root compatibility facades. Package/dataset/structured API tests and `run_tests.ps1` passed: 1016 tests, 1 skipped. |

| 9 | Phase 2 | done | Extract remaining health/readiness/model-status/metrics route registration and dependency wiring from `runtime_multi.py`. | Added `api.status_runtime` with deferred configuration providers so public route payloads retain request-time runtime reads. Status/quality/production tests and `run_tests.ps1` passed: 1017 tests, 1 skipped. |

| 10 | Phase 2 | done | Extract process/SSE route handler body from `runtime_multi.py` into `clashroyale_agent.api.process_routes`, preserving event order and payloads. | Added `ProcessRuntimeDependencies` and `handle_process_request`; root process entry remains a request-time compatibility facade, including the historical `split_stream_chunks` export. Process/SSE, disconnect, quota, package-boundary tests and `run_tests.ps1` passed: 1018 tests, 1 skipped. |

| 11 | Phase 2 | done | Reduce API app creation and route registration in `runtime_multi.py` to compatibility wiring plus dependency assembly; keep `run_api.ps1` unchanged. | Added `api.runtime` with `RuntimeAppDependencies` and centralized non-process route registration. Root runtime supplies request-time callbacks and keeps direct compatibility symbols pending QA slices #12-20; `run_api.ps1`/`run_backend.ps1` are unchanged. Route tests and `run_tests.ps1` passed: 1019 tests, 1 skipped. |

| 12 | Phase 2 | done | Split `query_parser.py` schema/constants into package modules under `clashroyale_agent.qa`, preserving fallback output contracts. | Added `qa.parser_schema` for the parser prompt, confidence levels, tower entities, and multi-intent cap; `query_parser` retains compatibility exports. Parser intent/confidence/multi-intent tests and `run_tests.ps1` passed: 1020 tests, 1 skipped. |

| 13 | Phase 2 | done | Split `query_parser.py` local normalization and alias helpers into package modules with focused parser regression tests. | Added `qa.card_aliases.CardAliasResolver` for normalization, cached alias construction, boundary-aware matching, and ordered resolution; root parser retains compatibility bindings and alias data ownership. Alias/parser/multi-intent tests and `run_tests.ps1` passed: 1021 tests, 1 skipped. |

| 14 | Phase 2 | done | Split `query_parser.py` fallback parser orchestration from model-parser orchestration, preserving timeout and fallback semantics. | Added `qa.parser_orchestration` with explicit local-parser/model dependencies; `runtime_multi.parse_user_query` remains a compatibility facade, including its model-call patch point. Timeout, invalid-response, reconciliation, and validated-result paths are preserved. Targeted tests and `run_tests.ps1` passed: 1022 tests, 1 skipped. |

| 15 | Phase 2 | done | Reduce `query_parser.py` to a compatibility wrapper once packaged parser modules own the implementation. | 2026-08-18 heartbeat: added `qa.parser_fallback` and `qa.parser_multi_intent`; root `query_parser.py` is 540 lines and now delegates fallback parse assembly, multi-intent decomposition, and multi-intent normalization through explicit dependency bundles. Targeted parser tests passed; `run_tests.ps1` passed with 1032 tests, 1 skipped. |

| 16 | Phase 2 | done | Split `query_answering.py` intent routing into `clashroyale_agent.qa` without changing direct-answer behavior. | 2026-08-18 heartbeat: added `qa.answer_routing` for RAG routing predicates, subquery titles, and subquery user-text normalization; `query_answering.py` and `runtime_multi.py` keep compatibility entry points as thin delegators. Targeted routing/query/multi-intent/open-analysis tests passed; `run_tests.ps1` passed with 1039 tests, 1 skipped. |

| 17 | Phase 2 | done | Split `query_answering.py` structured-answer orchestration from RAG orchestration with contract tests. | 2026-08-18: added `qa.structured_answering` with injected planner/executor dependencies and updated the root `answer_query` single-intent path to delegate through it while keeping fallback text and metadata contracts. Targeted structured/routing/query/open-analysis tests passed; `run_tests.ps1` passed with 1041 tests, 1 skipped. |

| 18 | Phase 2 | done | Split `query_answering.py` evidence ledger and grounding validation into a package module. | 2026-08-18 heartbeat: evidence ledger, reference suffixing, grounded stream buffer creation, completed-answer filtering, and ledger grounding validation live in `qa.evidence_grounding`; targeted evidence/streaming/synthesis tests passed. |

| 19 | Phase 2 | done | Split `query_answering.py` synthesis fallback and streaming presentation into package modules. | 2026-08-18 heartbeat: added `qa.synthesis_fallbacks` for snapshot/retrieved-evidence fallbacks and `qa.presentation` for stable chunked content emission; root imports keep compatibility. Targeted QA tests passed; `run_tests.ps1` passed with 1048 tests, 1 skipped. |

| 20 | Phase 2 | done | Reduce `query_answering.py` to a compatibility wrapper after packaged QA modules own the implementation. | 2026-08-18 heartbeat: `query_answering.py` is now 517 lines and delegates RAG answering to `qa.rag_answering`, evidence synthesis to `qa.evidence_synthesis`, multi-intent execution/composition to `qa.multi_intent_answering`, plus existing reviewer/retrieval/trace/structured-answer package modules. Root compatibility patch points are preserved for historical tests. Targeted QA/RAG tests passed; `run_tests.ps1` passed with 1056 tests, 1 skipped. |

| 21 | Phase 2 | done | Split `supercell_live.py` API client and request/rate/preflight helpers into `clashroyale_agent.collection`. | 2026-08-18 heartbeat: added `collection.api_client.OfficialAPIRequester` for bearer auth, timeout, pacing, Retry-After cooldown, JSON validation, metrics, and legacy `_get_json`; `SupercellAPIClient` now inherits it while preserving `supercell_live.requests.Session` patch semantics. Package/client tests and `test_supercell_live_data` passed; `run_tests.ps1` passed with 1060 tests, 1 skipped. |

| 22 | Phase 2 | done | Split `supercell_live.py` battle parsing into a collection parser module with synthetic fixtures only. | 2026-08-18 heartbeat: added `collection.battle_parser` for Path of Legend filtering, normalized battle records, opponent tag expansion, and usable-battle selection; root `supercell_live.py` keeps compatibility exports. Targeted collection/Supercell tests passed; `run_tests.ps1` passed with 1062 tests, 1 skipped. |

| 23 | Phase 2 | done | Split `supercell_live.py` deck/loadout normalization into a collection normalization module. | 2026-08-18 heartbeat: added `collection.loadout_normalization` for loadout card/side normalization, canonical loadouts, signatures, payloads, and quality scoring; root `battle_loadout.py` remains a compatibility wrapper with `app_config` path bootstrap. Targeted collection/Supercell/rolling-corpus tests passed; `run_tests.ps1` passed with 1063 tests, 1 skipped. |

| 24 | Phase 2 | done | Move rolling corpus/materializer implementation behind `clashroyale_agent.collection` modules while keeping scripts as thin entry points. | 2026-08-18 heartbeat: root `rolling_corpus.py`, `rolling_materializer.py`, and `scripts/collect_rolling_corpus.py` are compatibility entry points for packaged collection modules; added `collection.rolling_collector` and preserved legacy patch/import identities. Targeted collection/rolling tests passed: 49 tests. |

| 25 | Phase 2 | done | Verify collector script and Windows scheduled-task entry compatibility by text-level tests only; do not restart or modify live tasks. | 2026-08-18 heartbeat: added text-level coverage that the legacy collector script is only a packaged entry point and that Windows runners/installers still target the legacy script and existing runner chain. Targeted entry/schedule tests passed (56 tests); full public gate passed (1067 tests, 1 skipped). |

| 26 | Phase 2 / 4 | done | Move `web_ui_template.py` content into `src/clashroyale_agent/web/templates` and/or `static` without changing rendered UI. | 2026-08-18 heartbeat: moved the rendered HTML into `web/templates/index.html`; `web.template_loader` owns the file-backed `HTML_PAGE` and root `web_ui_template.py` is a compatibility module alias. Template identity and visualization tests passed; full unittest discovery passed with 1070 tests, 1 skipped. |

| 27 | Phase 2 | done | Split `web_app.py` API proxy, SSE rendering, static/template serving, and app startup into package modules. | 2026-08-18 heartbeat: packaged schemas, HTTP proxy helpers, SSE payload/forwarding, template loader, and app runtime now own those concerns; root `web_app.py` remains a 269-line route compatibility surface. Added contract tests for models, proxy status/envelopes, SSE payloads, and runtime creation. `run_tests.ps1` passed: 1079 tests, 1 skipped. |

| 28 | Phase 4 | done | Final README slimming pass: keep only project purpose, quick start, core links, data boundary, and high-level layout. | 2026-08-18 heartbeat: reduced README to project purpose, core capabilities, quick start, local launch, data boundary, high-level layout, documentation ownership links, and license. Removed duplicate API/testing/deployment/runbook detail. `check_repo.ps1 -SkipTests -SkipRuff` passed with 0 tracked private/generated paths. |

| 29 | Phase 4 | done | Deduplicate `00_START_HERE.md`, `docs/OPERATIONS.md`, and collection handoff docs so startup, recovery, and operations each have one owner. | 2026-08-18 heartbeat: `00_START_HERE.md` now owns only local API/Web startup and safe restart; `OPERATIONS.md` owns environment, exposure, deployment, host and troubleshooting boundaries; `SNAPSHOT_COLLECTION_HANDOFF.md` remains the sole collector task/recovery/PushPlus command owner. `check_repo.ps1 -SkipTests` passed with 0 tracked private/generated paths (Ruff advisory skipped because unavailable). |

| 30 | Phase 4 | done | Align `docs/ARCHITECTURE.md`, `docs/DATA_CONTRACT.md`, and `docs/RAG_AND_QA.md` with the actual package boundaries after each completed split. | 2026-08-18 heartbeat: documented package ownership for API, Web, QA, collection, stats, snapshots and ops while retaining root compatibility-entry boundaries and unchanged public contracts. `check_repo.ps1 -SkipTests` passed with 0 tracked private/generated paths (Ruff advisory skipped because unavailable). |

| 31 | Phase 4 | done | Add or update ADR index entries for package migration, private data boundaries, quality gates, and behavior-preserving refactor policy. | 2026-08-18 heartbeat: added ADR-015 covering package ownership, compatibility facades, private/generated data boundaries, deterministic gates, document ownership, and candidate-first cleanup; indexed and linked it from the ADR guide. `check_repo.ps1 -SkipTests` passed with 0 tracked private/generated paths (Ruff advisory skipped because unavailable). |

| 32 | Phase 5 | done | Produce `docs/reports/dead_code_candidates.md` from static references, docs references, scripts, and entry-point names; classify delete/confirm/keep. | 2026-08-18 heartbeat: added a static-reference audit with explicit removable/confirm/keep classifications. No tracked output or business file is unambiguously safe to delete; collector and compatibility boundaries are retained. `check_repo.ps1 -SkipTests` passed with 0 tracked private/generated paths (Ruff advisory skipped because unavailable). |

| 33 | Phase 5 | done | Clean one confirmed dead-output theme only, after candidate review; keep business code and collector compatibility untouched. | 2026-08-19: read-only Windows task audit found only the two active parallel tasks; old task names are absent. Removed only the unreferenced `client.py` local SSE debug client, updated the candidate report, and added a static regression test. No collector, schedule, API, or RAG entry point was changed. |
| 99 | Release readiness | done | Isolate default tests from local trace/SQLite/Qdrant runtime state and make root snapshot/stat compatibility modules bootstrap `app_config` before package imports. | 2026-08-19: test support injects temporary trace, feedback SQLite, RAG report, and Qdrant paths; `hybrid_retriever` honors the test Qdrant root; `snapshot_audit.py`, `snapshot_store.py`, `structured_query.py`, and `structured_stats.py` bootstrap through root `app_config`. Full unittest passed 1141 tests, OK, skipped=1; targeted bootstrap/trace/feedback/snapshot/stat tests, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |

| 34 | Phase 6 | done | Add `evaluation/reports/README.md` and document generated reports, benchmark commands, and no-commit rules. | 2026-08-18 heartbeat: added report classes, deterministic/live command guidance, retention/redaction rules, and no-commit checks; adjusted `.gitignore` to track only this README while ignoring generated children. `check_repo.ps1 -SkipTests` passed with 0 tracked private/generated paths. |

| 35 | Phase 6 | done | Standardize RAG benchmark reporting for retrieval latency, candidate count, and hit-rate fields. | 2026-08-18 heartbeat: retained legacy `latency_ms` and `metrics`, added `retrieval_latency_ms`, candidate-count summary, and `hit_rate_at_k` aliases per variant. Contract test added; `run_tests.ps1` passed with 1079 tests, 1 skipped. |

| 36 | Phase 6 | done | Standardize QA latency reporting for first-token time, total time, timeout rate, and fallback rate. | 2026-08-18 heartbeat: added explicit `timeout_rate` and `fallback_rate` scorecard fields, preserved first/total latency, and normalized those fields from QA result rows without retaining question text. Added 2 contract tests; `run_tests.ps1` passed with 1080 tests, 1 skipped. |

| 37 | Phase 6 | done | Standardize collector batch baseline reporting for batch duration, pre/post dedupe increments, and staging size. | 2026-08-18 heartbeat: added additive `batch_baseline` to collector results with total duration, raw/post-dedupe/inserted increments, and staging size/limit; retained all existing result fields. Contract test added; `run_tests.ps1` passed with 1081 tests, 1 skipped. |

| 38 | Phase 6 | done | Standardize API startup and RAG preheat baseline reporting, then link the metrics from operations docs. | 2026-08-18 heartbeat: added API startup and latest RAG preheat elapsed/outcome baselines to `/health` and documented their safe aggregate contract in `docs/OPERATIONS.md`; startup/preheat/status tests added. `run_tests.ps1` passed with 1083 tests, 1 skipped. |

| 39 | Final review | done | Run full public gate, inspect private-data status, update docs with final remaining risks, and summarize next phase if any. | 2026-08-18 heartbeat: full public gate passed with 1083 tests, 1 skipped; tracked private/generated path scan remains 0 and worktree status contains only source/docs/test/config paths. Health plan now records remaining oversize facades, advisory tooling, blocked candidate cleanup, aggregate-only baselines, and pre-existing CRLF whitespace found by the optional unstaged scan. |


## Post-Review Queue - 2026-08-18

This compact queue tracks the small follow-up items raised after the repository
review. It is intentionally separate from the main refactor queue so these
tooling and documentation fixes do not become another broad migration pass.

| # | Status | Slice | Evidence / notes |
|---:|---|---|---|
| A | done | Harden `scripts/check_repo.ps1` for Git safe.directory / dubious ownership failures. | Added explicit `git rev-parse --show-toplevel` root resolution and concise safe.directory failure text; focused script test covers the guard. |
| B | done | Wire the CI quality gate to compile package code and run repository hygiene without live providers. | `.github/workflows/ci.yml` now compiles `src` explicitly and runs `./scripts/check_repo.ps1 -SkipTests -SkipRuff`; tooling-config test covers both checks. |
| C | done | Document the local pre-test rule for stopping API/Web when local Qdrant is active. | `docs/TESTING.md` now records the 8080/8091 stop-before-full-unittest rule and PowerShell command to avoid false Qdrant file-lock failures. |
| D | done | Record and confirm the Apache-2.0 license choice. | 2026-08-19: owner confirmed Apache-2.0; `pyproject.toml` now uses SPDX metadata, while the existing `LICENSE` text and README link remain unchanged. |
| E | done | Refresh this queue with the compact post-review cleanup ledger. | Added this post-review section so future heartbeat runs can continue from a small, reviewable list. |
| F | done | Prepare the next small Phase 2 slice plan for remaining monoliths, prioritizing `supercell_live.py` API-client/preflight separation if it stays under about five files with tests. | 2026-08-18 post-review heartbeat: inspected current monolith sizes and Supercell collection boundaries. API client/preflight are already packaged, so the next safe candidate is the disk-backed snapshot workspace extraction below. No runtime or collector behavior changed. |

## Next Phase 2 Candidate Plan

The next implementation should remain a narrow behavior-preserving slice. Do
not combine it with collector strategy changes, scheduled-task edits, dead-code
cleanup, docs rewrites, or performance benchmark changes.

| # | Phase | Status | Slice | Acceptance / verification |
|---:|---|---|---|---|
| 40 | Phase 2 | done | Extract `JsonlRecordSequence` and `DiskBackedSnapshotWorkspace` from `supercell_live.py` into `src/clashroyale_agent/collection/snapshot_workspace.py`, leaving root imports as compatibility aliases. | 2026-08-19 heartbeat: moved the JSONL sequence and disk-backed workspace implementations into `collection.snapshot_workspace`; `supercell_live.py` retains compatibility exports and patch points. `test_supercell_live_data` and `test_collection_package_migration` passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 43 | Phase 2 | done | Move `runtime_multi.py` lifespan orchestration into `clashroyale_agent.api.lifespan` while keeping the root `lifespan` entry point and dependency patch points stable. | 2026-08-19 heartbeat: added `RuntimeLifespanDependencies`/`runtime_lifespan`; root wrapper constructs dependencies from existing module globals. `test_query_logic`, `test_deployment_roles`, and `test_api_lifecycle` passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 44 | Phase 2 | done | Move rolling-corpus write-lock and batch-policy primitives into `clashroyale_agent.collection.corpus_policy` while keeping root exports stable. | 2026-08-19 heartbeat: `CorpusWriterLock`, corpus policy errors, and `BatchValidationPolicy` now have a package owner; `rolling_corpus.py` re-exports the same objects. `test_rolling_corpus` and `test_collection_package_migration` passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 45 | Phase 2 | done | Move snapshot archive manifest scanning and retention deletion policy into `clashroyale_agent.snapshots.retention` while keeping `snapshot_store.cleanup_snapshot_retention` stable. | 2026-08-19 heartbeat: packaged archive manifest filtering and active/previous retention policy with injected timestamp/config dependencies; root wrapper preserves constants and public return fields. `test_daily_snapshot_store`, `test_snapshot_audit`, and `test_rag_preheat` passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 46 | Phase 2 | done | Move rolling-corpus timestamp, tag, battle, fact, and loadout-pair normalization into `clashroyale_agent.collection.corpus_normalization` while keeping root helper bindings stable. | 2026-08-19 heartbeat: pure normalization helpers now have a package owner; `rolling_corpus.py` retains the historical underscore aliases used by the store and tests. `test_rolling_corpus` (20) and `test_collection_package_migration` (11) passed; compileall and `git diff --check` passed. |
| 47 | Phase 2 | done | Move rolling-corpus SQLite observation and loadout persistence primitives into `clashroyale_agent.collection.corpus_observations` while keeping `RollingCorpusStore` method signatures and conflict semantics stable. | 2026-08-19 heartbeat: `insert_fact_observation` and `upsert_loadout` now own the SQL persistence logic; class methods remain compatibility wrappers and preserve injected behavior. `test_rolling_corpus` (21), `test_rolling_materializer` (2), and `test_collection_package_migration` (11) passed; compileall and `git diff --check` passed. |
| 48 | Phase 2 | done | Move rolling-materializer filesystem, identity, atomic JSON, and snapshot-group pruning primitives into `clashroyale_agent.collection.materializer_primitives`. | 2026-08-19 heartbeat: materializer helpers now have a package owner while `rolling_materializer.py` keeps historical underscore bindings. `test_rolling_materializer` (3) and `test_collection_package_migration` (11) passed; compileall and `git diff --check` passed. |
| 49 | Phase 2 | done | Move snapshot evidence formatting primitives into `clashroyale_agent.snapshots.evidence_primitives` while keeping `snapshot_store.py` helper names and RAG document fields stable. | 2026-08-19 heartbeat: metadata injection, raw deck normalization, archetype naming, percentages, and counter summaries now have a package owner. `test_daily_snapshot_store` (20), `test_retrieval_postprocess` (6), and `test_rag_preheat` (13) passed; compileall and `git diff --check` passed. |
| 41 | Phase 2 | superseded | Plan a separate `SupercellAPIClient.fetch_snapshot` orchestration split only after #40 passes. | Superseded 2026-08-19: later package-boundary slices and the final source-size audit completed the safe Supercell/runtime split; no separate fetch rewrite is required. |
| 42 | Phase 2 | superseded | Plan live snapshot/stat builder extraction only after fetch orchestration has a smaller surface. | Superseded 2026-08-19: `live_client.py`/`live_snapshot.py` own the extracted responsibilities and the final audit reports zero modules over 600 lines; preserve current compatibility facades. |
| 50 | Phase 2 | done | Move snapshot aggregate RAG evidence derivation into `clashroyale_agent.snapshots.aggregate_evidence` while keeping document IDs, fields, limits, and evidence-boundary behavior stable. | 2026-08-19 heartbeat: packaged `_build_aggregate_evidence_documents` behind an injected-configuration owner; `snapshot_store.py` retains the compatibility wrapper. `test_daily_snapshot_store` (21), `test_retrieval_postprocess` (6), and `test_rag_preheat` (13) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 51 | Phase 2 | done | Move structured-query input validation and presentation primitives into `clashroyale_agent.stats.query_contracts` while preserving repository methods and error contracts. | 2026-08-19 heartbeat: card/deck/full-loadout validation, low-sample warnings, loadout display, and card row projection now have a package owner; `StructuredStatsRepository` retains compatibility methods. `test_structured_stats` (11) and `test_structured_api` (10) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 52 | Phase 2 | done | Move structured-stat filesystem, JSON, snapshot identity, and atomic publication primitives into `clashroyale_agent.stats.build_primitives`. | 2026-08-19 heartbeat: publishing helpers now have a package owner while `structured_stats.py` retains compatibility wrappers and error translation. `test_structured_stats` (12) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 53 | Phase 2 | done | Move runtime model construction and committed card-catalog loading into `clashroyale_agent.qa.runtime_model` while preserving root names and patch points. | 2026-08-19 heartbeat: model/agent/catalog responsibilities now have a package owner; `runtime_multi.py` retains compatibility bindings. `test_query_logic` (34) and `test_parser_confidence` (14) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 54 | Phase 2 | done | Move snapshot-audit identity, JSON, hashing, and atomic publish primitives into `clashroyale_agent.snapshots.audit_primitives`. | 2026-08-19 heartbeat: local audit exports now use a package-owned primitive module while `snapshot_audit.py` preserves compatibility wrappers and review/import behavior. `test_snapshot_audit` (5) passed; compileall and `git diff --check` passed. |
| 55 | Phase 2 | done | Move rolling-collector status payload, batch baseline, concurrency, and heartbeat reporter responsibilities into `clashroyale_agent.collection.collector_status`. | 2026-08-19 heartbeat: collector status helpers now have a package owner while `rolling_collector.py` preserves historical names and entrypoint behavior. `test_collect_rolling_corpus` (22) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 56 | Phase 2 | done | Move rolling-collector staging-lane filesystem helpers and API-token parsing into `collector_staging.py` and `collector_tokens.py`. | 2026-08-19 heartbeat: lane sizing/state transitions and token parsing now have package owners; root collector names and token-slot behavior remain compatible. `test_collect_rolling_corpus` (22) passed; compileall and `git diff --check` passed. |
| 57 | Phase 2 | done | Move evidence-synthesis default model/retrieval/watchdog adapters and strict contract constants into `qa/synthesis_defaults.py` and `qa/synthesis_contracts.py`. | 2026-08-19 heartbeat: default adapters and strict error/prompt contracts now have package owners; dependency injection fields and synthesis behavior remain stable. `test_evidence_synthesis_skill` (21) and `test_open_analysis_pipeline` (22) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 58 | Phase 2 | done | Move snapshot-audit review JSONL parsing, numeric claim normalization, file verification, and external-review report construction into `snapshots.review_validation`. | 2026-08-19 heartbeat: review validation now has a package owner while `snapshot_audit.py` preserves import/rejection contracts. `test_snapshot_audit` (5) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 59 | Phase 2 | done | Move structured-query read-only SQLite connection scoping and provenance projection into `stats.query_context`. | 2026-08-19 heartbeat: dataset-scope temp views and provenance payload construction now have a package owner; `StructuredStatsRepository` preserves query methods and output fields. `test_structured_stats` (12) and `test_structured_api` (10) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 60 | Phase 2 | done | Move structured-stat result classification, Wilson lower-bound math, and counter accumulation into `stats.math_primitives`. | 2026-08-19 heartbeat: deterministic math helpers now have a package owner while `structured_stats.py` preserves historical wrappers. `test_structured_stats` (13) passed; compileall and `git diff --check` passed. |
| 61 | Phase 2 | done | Move snapshot-derived RAG corpus validation into `snapshots.rag_validation` while preserving failure codes and document contracts. | 2026-08-19 heartbeat: complete snapshot/document validation now has a package owner; `snapshot_store.validate_snapshot_rag_documents` remains a compatibility wrapper. `test_daily_snapshot_store` (22), `test_rag_preheat` (13), and `test_retrieval_postprocess` (6) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 62 | Phase 2 | done | Move structured-query card catalog and ranking projections into `stats.card_queries`. | 2026-08-19 heartbeat: card catalog/ranking output now has a package owner; repository methods preserve sort, rank, translation, and provenance fields. `test_structured_stats` (14) and `test_structured_api` (10) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 63 | Phase 2 | done | Move published-snapshot loading and derived-file compatibility migration into `snapshots.published_loader`. | 2026-08-19 heartbeat: canonical snapshot loading, card-deck migration, RAG revalidation, fingerprint repair, and CRLF normalization now have a package owner; `snapshot_store.load_published_snapshot` remains the compatibility wrapper. `test_daily_snapshot_store` (22) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `snapshot_store.py` reduced to 875 lines. |
| 64 | Phase 2 | done | Move structured-stat table finalization writes into `stats.finalize` while preserving card, deck, archetype, and matchup outputs. | 2026-08-19 heartbeat: fixed-card, deck, and archetype finalization now have package owners; `structured_stats.py` retains compatibility wrappers and injected math/classifier behavior. `test_structured_stats` (15) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_stats.py` reduced to 913 lines. |
| 65 | Phase 2 | done | Move runtime snapshot-lifecycle dependency wiring into `qa.runtime_dependencies`. | 2026-08-19 heartbeat: lifecycle dependency assembly now has a package owner while `runtime_multi._snapshot_lifecycle_dependencies` stays as the compatibility patch point. `test_api_package_boundaries` (9) and `test_deployment_roles` (3) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `runtime_multi.py` reduced to 1088 lines. |
| 66 | Phase 2 | done | Move runtime dataset dependency wiring into `qa.runtime_dependencies`. | 2026-08-19 heartbeat: dataset-scope repository/RAG dependency assembly now has a package owner while `runtime_multi._dataset_runtime_dependencies` stays as the compatibility patch point. `test_api_package_boundaries` (9), `test_deployment_roles` (3), and `test_supercell_live_data` (62) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `runtime_multi.py` reduced to 1058 lines. |
| 67 | Phase 2 | done | Move runtime status-route dependency wiring into `api.status_runtime`. | 2026-08-19 heartbeat: status providers, admin authorization, and metrics/readiness callbacks now have an API-package builder; `runtime_multi._status_route_dependencies` remains the compatibility patch point. `test_api_package_boundaries` (9), `test_status_routes` (2), and `test_production_hardening` (22) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `runtime_multi.py` reduced to 1038 lines. |
| 68 | Phase 2 | done | Move runtime `/process` dependency wiring into `api.process_routes`. | 2026-08-19 heartbeat: quota, metrics, request identity, trace redaction, answer builder, and SSE timing dependencies now have an API-package builder; `runtime_multi._process_runtime_dependencies` remains the compatibility patch point. `test_api_package_boundaries` (9) and `test_production_hardening` (22) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `runtime_multi.py` reduced to 1011 lines. |
| 69 | Phase 2 | done | Move runtime app assembly options into `api.runtime`. | 2026-08-19 heartbeat: non-process app configuration and route-option closures now have an API-package builder; `runtime_multi` keeps only the app factory call. `test_api_package_boundaries` (9), `test_production_hardening` (22), and `test_status_routes` (2) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `runtime_multi.py` reduced to 981 lines. |
| 70 | Phase 2 | done | Move runtime lifespan dependency wiring into `api.lifespan`. | 2026-08-19 heartbeat: startup/shutdown, feedback cache, quota, snapshot-follow, and preheat dependencies now have an API-package builder; `runtime_multi.lifespan` remains the compatibility patch point. `test_api_package_boundaries` (9), `test_production_hardening` (22), and `test_deployment_roles` (3) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `runtime_multi.py` reduced to 949 lines. |
| 71 | Phase 2 | done | Move runtime live-snapshot and readiness status wrappers into `api.status_runtime`. | 2026-08-19 heartbeat: live snapshot status and readiness payload wrappers now have API-package owners while `runtime_multi.get_live_snapshot_status` and `runtime_multi.get_readiness_status` stay as compatibility patch points. `test_api_package_boundaries` (9), `test_status_routes` (2), and `test_production_hardening` (22) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `runtime_multi.py` reduced to 929 lines. |
| 72 | Phase 2 | done | Move runtime RAG preheat dependency wiring into `api.rag_preheat`. | 2026-08-19 heartbeat: RAG docs, retriever factory, quality gate, retention, and preheat configuration dependencies now have an API-package builder; `runtime_multi.preheat_retriever` remains the compatibility patch point. `test_api_package_boundaries` (9), `test_rag_preheat` (13), and `test_production_hardening` (22) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `runtime_multi.py` reduced to 913 lines. |
| 73 | Phase 2 | done | Move structured-query card detail, comparison, pair, and teammate ranking projections into `stats.card_detail_queries`. | 2026-08-19 heartbeat: `card_stats`, `compare_cards`, `card_pair_stats`, and `card_teammate_rankings` now have a package owner while `StructuredStatsRepository` methods stay as compatibility wrappers. `test_structured_stats` (16) and `test_structured_api` (10) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_query.py` reduced to 826 lines. |
| 74 | Phase 2 | done | Move structured-stat SQLite schema creation into `stats.schema`. | 2026-08-19 heartbeat: table/index DDL now has a package owner while `structured_stats._schema` remains the compatibility wrapper. `test_structured_stats` (17) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_stats.py` reduced to 726 lines. |
| 75 | Phase 2 | done | Move structured-stat deck/loadout aggregate upsert primitives into `stats.write_primitives`. | 2026-08-19 heartbeat: deck, deck-matchup, full-loadout, and full-loadout-matchup writes now have a package owner while the four `structured_stats` helpers remain compatibility wrappers. `test_structured_stats` (18) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_stats.py` reduced to 677 lines. |
| 76 | Phase 2 | done | Move full-loadout feature accumulation and table finalization into `stats.loadout_stats`. | 2026-08-19 heartbeat: tower/evolution/elite/entity accumulation and final SQLite writes now have a package owner while `structured_stats` keeps compatibility wrappers and injected math helpers. `test_structured_stats` (19) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_stats.py` reduced to 557 lines and is below the 600-line target. |
| 77 | Phase 2 | done | Move rolling-corpus batch and publication-generation lifecycle writes into `collection.corpus_lifecycle`. | 2026-08-19 heartbeat: batch create/status/unique-id and publication generation start/finish writes now have a package owner while `RollingCorpusStore` keeps compatibility wrappers. `test_rolling_corpus` (22) and `test_collection_package_migration` (11) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 78 | Phase 2 | done | Move rolling-corpus SQLite schema creation into `collection.corpus_schema`. | 2026-08-19 heartbeat: rolling-corpus tables and indexes now have a package owner while `RollingCorpusStore._create_schema` remains a compatibility wrapper. `test_rolling_corpus` (23) and `test_collection_package_migration` (11) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. |
| 79 | Phase 2 | done | Move rolling-corpus workspace and legacy archive import orchestration into `collection.corpus_imports`. | 2026-08-19 heartbeat: workspace and legacy archive imports now have a package owner while `RollingCorpusStore` signatures and accepted record semantics remain unchanged. `test_rolling_corpus` (24) and `test_collection_package_migration` (11) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `rolling_corpus.py` reduced to 702 lines. |
| 80 | Phase 2 | done | Move rolling-corpus batch finalization validation and persistence into `collection.corpus_finalize`. | 2026-08-19 heartbeat: coverage/failure evaluation, batch status persistence, and rejected-batch cleanup now have a package owner while `RollingCorpusStore.finalize_batch` preserves output and failure codes. `test_rolling_corpus` (25) and `test_collection_package_migration` (11) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `rolling_corpus.py` reduced to 591 lines and is below the 600-line target. |
| 81 | Phase 2 | done | Move rolling-materializer meta-delta computation into `collection.materializer_deltas`. | 2026-08-19 heartbeat: meta-delta table creation, Wilson significance checks, base/full-loadout divergence, and delta-ready flags now have a package owner. `test_rolling_materializer` (4) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `rolling_materializer.py` reduced to 746 lines. |
| 82 | Phase 2 | done | Move rolling-materializer RAG document assembly and validation into `collection.materializer_documents`. | 2026-08-19 heartbeat: scope RAG document IDs/texts/metadata, source limits, scope coverage validation, and fingerprints now have a package owner. `test_rolling_materializer` (5) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `rolling_materializer.py` reduced to 375 lines and `materializer_documents.py` is 396 lines. |
| 83 | Phase 2 | done | Move snapshot publication orchestration into `snapshots.publisher` while preserving atomic writes, scope checks, streamed-work cleanup, and compatibility patch points. | 2026-08-19 heartbeat: publication orchestration now has a package owner; `snapshot_store.publish_daily_snapshot` preserves validation, derived files, pointer semantics, and streamed cleanup. `test_daily_snapshot_store` (23) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `snapshot_store.py` reduced to 790 lines. |
| 84 | Phase 2 | done | Move snapshot RAG document assembly into `snapshots.documents` while preserving document IDs, metadata, sort order, and evidence boundaries. | 2026-08-19 heartbeat: snapshot/card/deck/matchup document assembly now has a package owner; complete-snapshot gating and aggregate-evidence delegation remain compatibility-injected. `test_daily_snapshot_store` (24) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `snapshot_store.py` reduced to 706 lines. |
| 85 | Phase 2 | done | Move snapshot atomic JSON/file/archive primitives into `snapshots.storage` while preserving streamed serialization, fsync/replace semantics, and archive manifests. | 2026-08-19 heartbeat: atomic JSON, streamed serialization, file copy, and archive manifest helpers now have a package owner; `snapshot_store` wrappers and publication injection points remain stable. `test_daily_snapshot_store` (25) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `snapshot_store.py` reduced to 654 lines. |
| 86 | Phase 2 | done | Move collector snapshot summary and active-summary loading into `snapshots.status_summary` while preserving pointer validation and private raw-battle omission. | 2026-08-19 heartbeat: compact summary projection and pointer/archive loading now have a package owner; private raw battles remain omitted and compatibility wrappers are stable. `test_daily_snapshot_store` (26) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `snapshot_store.py` reduced to 618 lines. |
| 87 | Phase 2 | done | Move snapshot identity, completeness, scope, fingerprint, and age/refresh helpers into `snapshots.identity`. | 2026-08-19 heartbeat: snapshot IDs, completeness checks, Path of Legend scope checks, RAG fingerprints, timestamp parsing, age, and refresh timing now have a package owner. `test_daily_snapshot_store` (27) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `snapshot_store.py` reduced to 570 lines and is below the 600-line target. |
| 88 | Phase 2 | done | Move structured loadout-entity catalog, ranking, lookup, resolution, and comparison queries into `stats.entity_queries`. | 2026-08-19 heartbeat: entity catalog/ranking/lookup/reference-resolution/comparison behavior now has a package owner; entity IDs, ordering, ranks, errors, warnings, and provenance remain compatibility-injected. `test_structured_stats` (20) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_query.py` reduced to 739 lines. |
| 89 | Phase 2 | done | Move structured full-loadout catalog projection into `stats.loadout_catalog`. | 2026-08-19 heartbeat: tower/card catalog projection now has a package owner; ordering, localized names, evolution/elite flags, readiness errors, and provenance remain stable. `test_structured_stats` (21) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_query.py` reduced to 692 lines. |
| 90 | Phase 2 | done | Move structured legacy answer-payload projection into `stats.answer_payload`. | 2026-08-19 heartbeat: legacy cards_meta/top_decks/card_deck_stats projection now has a package owner; rank/order, provenance, and rolling-scope compatibility shape remain stable. `test_structured_stats` (22) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_query.py` reduced to 631 lines. |
| 91 | Phase 2 | done | Move structured deck profile and exact matchup projections into `stats.deck_queries`. | 2026-08-19 heartbeat: canonical deck profile and exact-matchup projections now have a package owner; opponent ordering, perspective wins/losses, draw handling, rates, warnings, and provenance remain stable. `test_structured_stats` (23) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_query.py` reduced to 537 lines and is below the 600-line target. |
| 92 | Phase 2 | done | Move structured full-loadout profile and exact matchup projections into `stats.full_loadout_queries`. | 2026-08-19 heartbeat: full-loadout profile and exact matchup projections now have a package owner; normalization, opponent ordering, perspective wins/losses, draws, rates, warnings, errors, and provenance remain stable. `test_structured_stats` (24) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_query.py` reduced to 414 lines. |
| 93 | Phase 2 | done | Move structured archetype projection into `stats.archetype_queries`. | 2026-08-19 heartbeat: archetype ordering, family labels, representative decks, matched counts, and provenance now have a package owner. `test_structured_stats` (25) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `structured_query.py` reduced to 388 lines. |
| 94 | Phase 2 | done | Move runtime grounded-answer execution and result metadata assembly into `qa.runtime_answering`. | 2026-08-19 heartbeat: answer_query invocation, normalization, stream fallback, and grounded-result assembly now have a package owner; parser/RAG/live metadata and SSE behavior remain stable. `test_api_package_boundaries` (10) and `test_open_analysis_pipeline` (22) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `runtime_multi.py` reduced to 905 lines. |
| 95 | Phase 2 | done | Move runtime answer-data preparation and final result decoration into `qa.runtime_answering`. | 2026-08-19 heartbeat: data-context selection, rolling/live RAG metadata, preheated-retriever selection, and answer result metadata now have a package owner while parse events and the `build_answer` compatibility entry remain stable. `test_api_package_boundaries` (11) and `test_open_analysis_pipeline` (22) passed; compileall and `git diff --check` passed. `runtime_multi.py` reduced to 871 lines. |
| 96 | Phase 2 | done | Move runtime compatibility facade, answer parsing, and build-answer orchestration into owned API/QA modules. | 2026-08-19 heartbeat: snapshot/RAG/dataset compatibility delegates moved to `api.runtime_facade`; parse boundary moved to `qa.runtime_parsing`; full answer orchestration moved to `qa.runtime_pipeline`; live-card merge moved to `qa.runtime_data_context`. Public runtime names and patch-friendly dependency lookup remain stable. `test_api_package_boundaries` (15) passed, live-card compatibility test passed, compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. `runtime_multi.py` reduced to 600 lines. |
| 97 | Phase 2 | done | Run the final non-test source-size audit and close the giant-file target. | 2026-08-19 heartbeat: repository scan excluding `.venv`, `.git`, `tests`, and `evaluation` found zero Python source modules over 600 lines; `runtime_multi.py` is exactly 600 lines. `docs/REPO_HEALTH_PLAN.md` now records the reached target and future re-audit rule. |
| 98 | Phase 2 | done | Move evidence-synthesis dependency wiring into `qa.synthesis_dependencies` while retaining the existing public import. | 2026-08-19 heartbeat: the dependency dataclass and model/retrieval defaults now have a package owner; `evidence_synthesis.py` continues to re-export `EvidenceSynthesisDependencies`. `test_api_package_boundaries` (16) and `test_evidence_synthesis_skill` (21) passed; compileall, `git diff --check`, and `check_repo.ps1 -SkipTests -SkipRuff` passed. The source-size audit remains at zero modules over 600 lines. |
