services:
  prometheus:
    volumes:
      - ./deploy/prometheus.drill.yml:/etc/prometheus/prometheus.yml:ro
      - ./deploy/prometheus-alerts-drill.yml:/etc/prometheus/prometheus-alerts.yml:ro
    ports:
      - "127.0.0.1:9090:9090"
